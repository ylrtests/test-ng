apply plugin: "war"

bootWar {
    mainClassName = "com.electronicarmory.armory.ArmoryApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
